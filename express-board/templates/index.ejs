<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>留言板列表</title>
</head>
<body>
  <section>
    <a href="/publish">留言板列表</a>
  </section>
  <section>
    <% if(messages.length === 0) { %>
      <p>暂时没有留言内容</p>
    <% } else { %>
      <table>
        <tr>
          <th>留言时间</th>
          <th>留言姓名</th>
          <th>留言内容</th>
        </tr>
        <% messages.forEach((msg) => {%>
          <tr>
            <td><%=msg.time%></td>
            <td><%=msg.name%></td>
            <td><%=msg.content%></td>
          </tr>
        <% })%>
      </table>
    <% } %>
  </section>
</body>
</html>
